# --- core ---
pydantic>=2.6
PyYAML>=6.0
numpy>=1.26
tqdm>=4.66
httpx>=0.27
typer>=0.12
rich>=13.7

# --- document parsing ---
PyMuPDF>=1.24          # PDF text + layout + font metadata
ebooklib>=0.18         # EPUB
beautifulsoup4>=4.12
python-docx>=1.1       # DOCX in / question-paper export

# --- chunking ---
tiktoken>=0.7          # token-accurate chunk sizing

# --- retrieval ---
sentence-transformers>=3.0   # BGE-M3 embeddings + BGE cross-encoder reranker
torch>=2.2
rank-bm25>=0.2.2             # lexical retrieval

# --- ui ---
streamlit>=1.36
torchvision